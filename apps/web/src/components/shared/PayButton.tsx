// DEPRECATED — this component wrapped flutterwave-react-v3's inline modal
// checkout, which has no eTranzact equivalent (eTranzact's virtual-account
// model has no in-browser card/USSD modal — see @/components/shared/PaymentInstructions,
// the account-transfer instructions component that replaced this flow).
//
// Left in place (rather than deleted) only because this environment could
// not remove the file; safe to delete manually. Do not add new imports —
// use PaymentInstructions instead.
export {};
